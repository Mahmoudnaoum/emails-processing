# Requirements without any Rust dependencies
# Uses pure Python libraries only
# Install with: pip install -r requirements_no_rust.txt

# Database - Pure Python PostgreSQL driver (no compilation)
pg8000==1.30.5

# Environment variables
python-dotenv==1.0.0

# API - Flask-based (no pydantic required)
flask==3.0.0
flask-cors==4.0.0

# Email processing
email-validator==2.1.0
